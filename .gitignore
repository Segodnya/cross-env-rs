# Rust
target/
Cargo.lock.bak
*.pdb

# Native binaries are staged into platform packages by CI before publish,
# never committed to git. npm pack still picks them up via the package's
# `files` field at publish time.
npm/cross-env-rs-*/bin/

# Node
node_modules/
.npmrc.local
*.tgz

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Local-only env / scratch
.env
.env.local
scratch/
