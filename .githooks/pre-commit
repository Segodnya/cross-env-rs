#!/usr/bin/env bash
# Local pre-commit checks. Wire up once per clone:
#   git config core.hooksPath .githooks
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

echo "[pre-commit] cargo fmt --check"
cargo fmt --all -- --check

echo "[pre-commit] cargo test --workspace"
cargo test --workspace --quiet

SHIM_DIR="npm/cross-env-rs/test"
if [ -d "$SHIM_DIR" ]; then
    SHIM_TESTS=$(find "$SHIM_DIR" -name '*.test.js' -type f)
    if [ -n "$SHIM_TESTS" ]; then
        echo "[pre-commit] node --test (shim)"
        # shellcheck disable=SC2086
        node --test $SHIM_TESTS
    fi
fi

echo "[pre-commit] ok"
